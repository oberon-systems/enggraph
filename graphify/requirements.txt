# Pinned on purpose. tree-sitter changed its Python API twice in the 0.2x line
# (Parser.set_language removed, Language.query replaced by Query/QueryCursor),
# so an unpinned rebuild used to break the indexer without a code change.
# The code extractor. Pulled in with the `mcp` extra so its stdio server can
# be started from this image; the neo4j and pdf extras stay out because
# nothing here uses them. Its tree-sitter pins agree with ours below, which is
# the only reason the two can share an environment.
graphifyy[mcp]==0.1.14
# graphifyy's stdio server registers its tools with `@server.list_tools()`,
# a decorator the MCP SDK dropped in 2.0. Its own `mcp` extra is unpinned, so
# the ceiling has to be set here or the server dies on import.
mcp<2
# Summaries are written at index time by a local GGUF model. PyPI carries this
# one as an sdist; the Dockerfile builds it in a stage of its own.
llama-cpp-python==0.3.7
# The worker API: the queue a machine with a GPU claims work from. Both ship
# manylinux wheels, so the builder stage compiles nothing new for them.
fastapi==0.121.1
uvicorn==0.35.0
pathspec==1.1.1
psycopg2-binary==2.9.12
# Ansible structure lives in the YAML values, not in the tree-sitter tree.
PyYAML==6.0.3
tree-sitter==0.26.0
tree-sitter-bash==0.25.1
tree-sitter-dockerfile==0.2.0
tree-sitter-embedded-template==0.25.0
tree-sitter-go==0.25.0
tree-sitter-hcl==1.2.0
tree-sitter-javascript==0.25.0
tree-sitter-json==0.24.8
tree-sitter-make==1.1.1
tree-sitter-markdown==0.5.1
tree-sitter-puppet==1.3.0
tree-sitter-python==0.25.0
tree-sitter-ruby==0.23.1
tree-sitter-rust==0.24.2
tree-sitter-toml==0.7.0
tree-sitter-typescript==0.23.2
tree-sitter-yaml==0.7.2

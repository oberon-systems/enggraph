[pytest]
# The package lives under a src root, the same way the image sets PYTHONPATH.
# This directory joins it so `tests.<module>` imports resolve under the
# console script too, which does not put the working directory on the path
# the way `python -m pytest` does.
pythonpath = src .
testpaths = tests
